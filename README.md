# wasi-sdk-gh-actions

A CI base Docker image that extends the official [`wasi-sdk`](https://github.com/WebAssembly/wasi-sdk) image with extra build tooling, so a single container can build WASI (wasm) targets.

Pull from GHCR: `ghcr.io/portable-network-archive/wasi-sdk-gh-actions`.
